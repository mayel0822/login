<div id="message-dropdown"
     class="hidden absolute right-0 top-full mt-2 w-80 bg-white rounded-2xl shadow-xl border border-gray-100 z-50 overflow-hidden">

    <!-- Header -->
    <div class="flex items-center justify-between px-4 py-3 border-b border-gray-100">
        <h3 class="text-sm font-bold text-[#1e3a5f]">Messages</h3>
        <span class="text-xs bg-[#1e3a5f] text-white px-2 py-0.5 rounded-full font-medium">2 new</span>
    </div>

    <!-- Message Items -->
    <ul class="divide-y divide-gray-50 max-h-72 overflow-y-auto">
        <li class="flex gap-3 px-4 py-3 hover:bg-gray-50 transition cursor-pointer">
            <div class="w-9 h-9 rounded-full bg-[#1e3a5f] flex items-center justify-center flex-shrink-0 text-white text-sm font-bold">
                L
            </div>
            <div class="flex-1 min-w-0">
                <div class="flex items-center justify-between">
                    <p class="text-sm font-semibold text-[#1e3a5f]">Librarian</p>
                    <p class="text-xs text-gray-400">2m ago</p>
                </div>
                <p class="text-xs text-gray-500 truncate">Your book reservation has been confirmed. Please pick it up before 3:30 PM.</p>
            </div>
            <span class="w-2 h-2 rounded-full bg-blue-500 flex-shrink-0 mt-2"></span>
        </li>

        <li class="flex gap-3 px-4 py-3 hover:bg-gray-50 transition cursor-pointer">
            <div class="w-9 h-9 rounded-full bg-red-400 flex items-center justify-center flex-shrink-0 text-white text-sm font-bold">
                S
            </div>
            <div class="flex-1 min-w-0">
                <div class="flex items-center justify-between">
                    <p class="text-sm font-semibold text-[#1e3a5f]">System</p>
                    <p class="text-xs text-gray-400">1h ago</p>
                </div>
                <p class="text-xs text-gray-500 truncate">Reminder: "Sapiens" must be returned by today 5:00 PM.</p>
            </div>
            <span class="w-2 h-2 rounded-full bg-blue-500 flex-shrink-0 mt-2"></span>
        </li>

        <li class="flex gap-3 px-4 py-3 hover:bg-gray-50 transition cursor-pointer">
            <div class="w-9 h-9 rounded-full bg-emerald-500 flex items-center justify-center flex-shrink-0 text-white text-sm font-bold">
                L
            </div>
            <div class="flex-1 min-w-0">
                <div class="flex items-center justify-between">
                    <p class="text-sm font-semibold text-[#1e3a5f]">Librarian</p>
                    <p class="text-xs text-gray-400">Yesterday</p>
                </div>
                <p class="text-xs text-gray-500 truncate">Your computer session has ended. Thank you for using the library.</p>
            </div>
        </li>
    </ul>

    <!-- Footer -->
    <div class="px-4 py-2 border-t border-gray-100 text-center">
        <a href="#" class="text-xs font-medium text-[#1e3a5f]/60 hover:text-[#1e3a5f] transition">View all messages</a>
    </div>
</div>
